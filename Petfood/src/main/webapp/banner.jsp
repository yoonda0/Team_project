<%@ page pageEncoding="UTF-8"%>
<section class="banner-wrapper">
	<a href="${param.bannerLink}" class="banner-link"
		title="${param.bannerCaption}" aria-label="${param.bannerCaption}">
		<img class="banner-img" alt="${param.bannerCaption}" 
		src="img/${param.bannerImage}">
	</a>
</section>